body {
    background: #121212;
    color: #fff;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
}
.casino-container {
    background: #1e1e1e;
    padding: 30px;
    border-radius: 15px;
    box-shadow: 0 0 20px rgba(255, 0, 128, 0.3);
    text-align: center;
    width: 350px;
}
.balance-display {
    font-size: 24px;
    color: #4cd137;
    font-weight: bold;
}
.slot-machine {
    display: flex;
    justify-content: space-between;
    background: #000;
    padding: 20px;
    border-radius: 10px;
    border: 4px solid #ff007f;
    margin: 20px 0;
}
.slot {
    background: #fff;
    font-size: 50px;
    width: 80px;
    height: 80px;
    line-height: 80px;
    border-radius: 5px;
    box-shadow: inset 0 0 10px rgba(0,0,0,0.5);
}
.controls input {
    width: 60px;
    padding: 8px;
    font-size: 16px;
    border-radius: 5px;
    border: none;
    text-align: center;
}
button {
    background: #ff007f;
    color: white;
    border: none;
    padding: 10px 20px;
    font-size: 18px;
    font-weight: bold;
    cursor: pointer;
    border-radius: 5px;
    margin-left: 10px;
    transition: 0.2s;
}
button:hover { background: #ff3399; }
button:disabled { background: #555; cursor: not-allowed; }
#message { font-size: 18px; margin-top: 20px; min-height: 24px; }
.blur { filter: blur(3px); } /* CSS voor het draai-effect */
